<nav class="nav nav-pills nav-fill">
  <a class="nav-link" aria-current="page" href="{{ path('app_recette') }}">Accueil</a>

    {% if app.user %}
        {% if is_granted('ROLE_ADMIN') %}
            <a class="nav-link" href="{{ path('admin') }}">Espace Admin</a>
            <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
            <a class="nav-link" href="{{ path('app_account') }}">Mon compte <small>({{ app.user.firstname }})</small></a>
        {% elseif  ('ROLE_USER') %}
            <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
            <a class="nav-link" href="{{ path('app_account') }}">Mon compte <small>({{ app.user.firstname }})</small></a>
            {% endif %}
        {% else %}
            <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
            <a class="nav-link" href="{{ path('app_register') }}">Inscription</a>
        
    {% endif %}
  <a class="nav-link" href="{{ path('app_recette') }}">Recettes</a>
  <a class="nav-link" href="{{ path('app_ingredient') }}">Ingredients</a>
  <a class="nav-link" href="{{ path('app_composition') }}">Composition</a>
  <a class="nav-link" href="#">Contact</a>
</nav>